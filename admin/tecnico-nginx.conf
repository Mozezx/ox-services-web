# /etc/nginx/sites-available/tecnico.oxservices.org
# Interface do técnico: Minhas obras, Loja, Pedidos, etc.

server {
    listen 443 ssl;
    server_name tecnico.oxservices.org;

    ssl_certificate /etc/letsencrypt/live/tecnico.oxservices.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/tecnico.oxservices.org/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 100M;

    root /var/www/ox-services-web/technician/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(webmanifest|json|ico|png|svg|woff2)$ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=86400";
    }

    location /api/ {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        alias /var/www/ox-services-web/backend/public/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

server {
    if ($host = tecnico.oxservices.org) {
        return 301 https://$host$request_uri;
    }

    listen 80;
    server_name tecnico.oxservices.org;
    return 404;
}
